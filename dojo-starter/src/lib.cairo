mod systems {
    mod actions;
}

mod models {
    mod moves;
    mod position;
    mod piece;
    mod team;
    mod game;
    // Retrieve the player's current position from the world.
}

mod tests {
    mod test_world;
}
